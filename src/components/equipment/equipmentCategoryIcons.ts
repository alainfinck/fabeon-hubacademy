import type { LucideIcon } from 'lucide-react'
import { Box, Droplets, Factory, Paintbrush, Printer, Scissors } from 'lucide-react'

export const equipmentCategoryIcons: Record<string, LucideIcon> = {
  Box,
  Printer,
  Droplets,
  Scissors,
  Paintbrush,
  Factory,
}
